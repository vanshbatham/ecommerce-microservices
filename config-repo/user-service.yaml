server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_user
    username: root
    password: Vansh@sql16
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true